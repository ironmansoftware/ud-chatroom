trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: pwsh -Command "& { Copy-Item ./ud-chatroom.psd1 $Env:Build_ArtifactStagingDirectory; Copy-Item ./ud-chatroom.psm1 $Env:Build_ArtifactStagingDirectory; Save-Module -Name UniversalDashboard -Path $Env:Build_ArtifactStagingDirectory/ud }"
  displayName: 'Copy and Download UDs'